/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'gflyiytagaulfyackemh.supabase.co' },
    ],
  },
}

module.exports = nextConfig
